freinds=["apples","oranges"]
print(freinds[0])
freinds[0]="bananas" # list is mutable
# This will change the first element of the list
print(freinds[0])
freinds.append("mangoes") # This will add a new element to the end of the 
print(freinds[1:2]) # This will print the second element of the list
l1=[2,1,5,4,3]
l1.sort() # This will sort the list in ascending order
print(l1) # This will print the sorted list
l1.reverse() # This will reverse the list
print(l1) # This will print the reversed list
l1.insert(2,6) # This will insert 6 at index 2
print(l1) # This will print the list after inserting 6
#tuples are immutable 
a=(1,)# This will create a tuple with one element
b=(1,2,3,"rohan",True ,456) # This will create a tuple with three elements
print(type(a)) # This will print the type of a
print(a.count(1)) # This will count the number of times 1 appears in the tuple a
print(b.index(456)) # This will return the index of 456 in the tuple b
print(2 in b) # This will check if 2 is in the tuple b
print(a+b) # This will concatenate the two tuples a and b
print(b[1:3]) # This will print the second and third elements of the tuple b
# problem Set
# 1.write a program to store seven fruit name in a list entered by the user
fruits = []
for i in range(7):
    fruit = input("Enter the name of the fruit: ")
    fruits.append(fruit)
print("The list of fruits is:", fruits)
# 2. write a program to accept marks of six students and display them in a sorted manner
marks = []
for i in range(6):
    mark = int(input("Enter the marks of the Student {}: ".format(i+1)))
    marks.append(mark)
marks.sort()
print("The sorted list of marks is:", marks)
