# Titanic Survival Prediction

Jupyter notebook: `titanic_survival_prediction.ipynb`

Description:
- Classification notebook that predicts passenger survival on the Titanic dataset.

How to run:
1. Open the notebook in Jupyter, JupyterLab, or Google Colab.
2. Install dependencies (example):

```
pip install scikit-learn pandas numpy matplotlib
```

Notes:
- Ready to be packaged into a separate repo with `scripts/package_repos.py`.
